api() {
    curl -s https://openrouter.ai/api/v1/credits \
        -H "Authorization: Bearer $(security find-generic-password -a "$USER" -s "OPENROUTER_API_KEY" -w)" \
        | jq -r '"OpenRouter: \((.data.total_credits - .data.total_usage) * 100 | round / 100)"'
}